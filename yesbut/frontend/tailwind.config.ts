import type { Config } from 'tailwindcss';

/**
 * Tailwind CSS Configuration for YesBut Frontend
 *
 * This configuration extends the default Tailwind setup with:
 * - Custom color palette for the YesBut design system
 * - Extended spacing and sizing utilities
 * - Custom animations for streaming indicators and graph transitions
 * - Typography plugin for markdown rendering
 */
const config: Config = {
  // TODO: Define content paths for Tailwind to scan
  // TODO: Define custom theme extensions (colors, spacing, fonts)
  // TODO: Define custom animations (typing indicator, node pulse, edge flow)
  // TODO: Configure dark mode strategy
  // TODO: Add plugins (typography, forms, aspect-ratio)
};

export default config;
